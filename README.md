# 🗺️ Dashboard GIS
### تحديث تنظيمات واشتراطات البناء – مكة المكرمة
**الهيئة الملكية لمدينة مكة المكرمة والمشاعر المقدسة**

---

## 📌 نبذة عن المشروع | About

لوحة تحكم تفاعلية مبنية بـ **R Shiny** تُتيح استعراض وتصفية وتحليل بيانات قطع الأراضي في مكة المكرمة بشكل مكاني على خريطة ساتلايت، مع عرض بيانات البناء والاشتراطات لكل قطعة.

An interactive GIS dashboard built with **R Shiny** for visualizing and analyzing land parcel data in Makkah Al-Mukarramah, including building regulations and property details.

---

## ✨ المميزات | Features

| الميزة | التفاصيل |
|--------|----------|
| 🗺️ خريطة ساتلايت | Esri WorldImagery + تسميات |
| 🎨 ألوان ديناميكية | تدرج لوني حسب **عدد الطوابق** |
| 🔥 Heatmap | كثافة حسب عدد الطوابق |
| 📍 تجميع النقاط | Marker Clustering |
| 🖱️ Popup تفاعلي | 4 أقسام: التعريف، البناء، الموقع، الإدارة |
| 🔍 فلاتر متعددة | استخدام الأرض / نوعها / الحي / حالة المسح |
| 📊 جدول بيانات | DataTable قابل للبحث والترتيب |
| 📈 رسم بياني | توزيع عدد الطوابق |
| ✏️ رسم على الخريطة | Draw Toolbar |

---

## 🏗️ هيكل البيانات | Data Structure

```
Shapefile: PAR.shp
الحقول الأساسية:
├── pkAddressI   → رقم القطعة
├── fkDistrict   → رمز الحي
├── LandUse      → استخدام الأرض
├── LandType     → نوع الأرض
├── NoofFloor    → عدد الطوابق ⭐
├── NoofUnit     → عدد الوحدات
├── Shape_Area   → المساحة (م²)
├── StreetArab   → اسم الشارع بالعربي
├── Restrictio   → الاشتراطات
├── GeoXCoordi   → خط الطول (WGS84)
├── GeoYCoordi   → خط العرض (WGS84)
└── DateCreate / DateModifi / LastUser / ...
```

---

## 🚀 تشغيل المشروع | How to Run

### المتطلبات | Requirements
- R ≥ 4.0
- المكتبات | Packages:

```r
install.packages(c("shiny", "sf", "dplyr", "leaflet",
                   "leaflet.extras", "DT", "bslib"))
```

### التشغيل | Run

```powershell
# من داخل فولدر المشروع
& "C:/Program Files/R/R-4.5.2/bin/Rscript.exe" -e "shiny::runApp('app.R', launch.browser=TRUE)"
```

أو من R Console:
```r
shiny::runApp("app.R", launch.browser = TRUE)
```

---

## 📂 هيكل المشروع | Project Structure

```
Dashboard_GIS/
├── app.R          # الكود الرئيسي للتطبيق
├── README.md      # هذا الملف
└── .gitignore     # ملفات مستثناة من Git
```

> ملاحظة: الـ Shapefile (`PAR.shp` وملحقاته) غير مرفوع لأسباب تتعلق بحجم الملف وخصوصية البيانات.

---

## 🛠️ التقنيات المستخدمة | Tech Stack

![R](https://img.shields.io/badge/R-276DC3?style=flat&logo=r&logoColor=white)
![Shiny](https://img.shields.io/badge/Shiny-blue?style=flat)
![Leaflet](https://img.shields.io/badge/Leaflet-199900?style=flat&logo=leaflet&logoColor=white)

| المكتبة | الاستخدام |
|---------|----------|
| `shiny` | إطار التطبيق التفاعلي |
| `sf` | قراءة وتحليل بيانات GIS |
| `leaflet` + `leaflet.extras` | الخريطة التفاعلية + Heatmap |
| `dplyr` | معالجة البيانات |
| `DT` | جدول البيانات التفاعلي |
| `bslib` | واجهة المستخدم Bootstrap |

---

## 📋 ملاحظات تقنية | Technical Notes

- تم استخدام `sf_use_s2(FALSE)` و `st_make_valid()` لمعالجة مشاكل صلاحية الـ geometry
- الـ Popup مبني بنظام **vectorized** لضمان الأداء السريع مع 14,000+ قطعة
- الألوان تعتمد على `colorBin` بتدرج من أصفر (طوابق قليلة) إلى أحمر داكن (طوابق كثيرة)

---

*PRJ-00852 | 2026*
